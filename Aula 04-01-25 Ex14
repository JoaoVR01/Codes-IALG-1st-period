#include <iostream>
#include <fstream>
using namespace std;

int main(){
    float a,b,area;
    
    ifstream entrada("triangulo.txt");
    entrada >> a >> b;
    area = (a*b)/2;
    ofstream saida("saida.txt");
    saida << area;
    saida.close();
    return 0;
}
